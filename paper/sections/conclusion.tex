\section{Conclusion}
\label{sec:conclusion}
% TODO: rewrite once results exist; keep to a short restatement of the
% three contributions from Section~\ref{sec:intro} plus one honest
% limitation (e.g. simplified RBDC/TBDC evaluation, single-hardware
% deployment measurement, linear-regime theory assumption in
% Section~\ref{sec:theory-gating}) and one concrete future-work item.
We presented a strictly causal streaming video anomaly detector built on
a causal diagonal state-space core with a learned event-boundary decay
gate, a theoretical analysis relating decay spectrum to detection delay,
and an evaluation that includes real on-device edge latency measurements
alongside standard accuracy metrics. Limitations and future work: [fill
in after ablations are run].
